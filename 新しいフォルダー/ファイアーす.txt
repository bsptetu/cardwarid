{
  "rules": {
    ".read": true,
    ".write": true
  }
}

上データーベース


rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

Storageの設定